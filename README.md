# UI FIX & CHANGES – SQL Server AI Analyst 

1. public/ (Static Assets Folder) - Added QFT-image.png: Placed the corporate logo here so Next.js could serve it globally via the /QFT-image.png path.
2. src/app/login/page.tsx (Login Page) - Layout Transformation: Completely stripped the original two-column dark mode layout and replaced it with the clean, centered white card UI. Input Standardization: Updated placeholder text to analyst@yourcompany.com and Enter your password. Autofill Fix: Added autoComplete="new-password" and autoComplete="off" to the form and inputs to prevent the browser from aggressively overlaying saved credentials during development.
3. src/app/dashboard/page.tsx (Main Dashboard) - Theme Overhaul: Converted the dark #111827 sidebar into a crisp #ffffff white panel and updated the main chat container to a light #f8fafc background. Sidebar Navigation: Styled the previous chat sessions to look like clean, hoverable menu items. Floating Toggle: Moved the < sidebar collapse button to float perfectly on the right border of the sidebar. Header Updates: Added the + New chat button directly next to the Quinte logo in the top left. Ollama Lifecycle: Restored the logic inside the Settings panel to correctly show the "Detecting Ollama models..." spinner, error states, and the dropdown list of local model sizes. Quick Prompts: Replaced the emojis with professional, minimal SVG icons. Attachment UI Fix: Fixed a logic bug where non-image files (like .txt or .csv) were rendering as broken image thumbnails and mapped specific file extensions to custom SVG document icons. Voice & Language Controls: Restored the Language dropdown, the microphone button, the live recording pulse animation, and the Ctrl+Shift+M keyboard shortcut hint in the input bar. Panel Icons: Updated the "Lineage" and "Cache" buttons in the top right, as well as the Semantic Cache drawer header, to use matching minimal SVGs instead of emojis.
4. src/components/chat/MessageBubble.tsx (Chat UI) - User Bubble Styling: Updated the background color of the user's message bubbles to the corporate dark blue (#111827) and set the text to white (#ffffff) for readability. Edit State: Tweaked the textarea styling used when editing a previous message to ensure it matches the new dark theme borders.
5. src/components/files/DataLineagePanel.tsx (Lineage Drawer) - Header Icon Update: Replaced the emoji in the panel's top title with the matching SVG path used in the top navigation bar.
